<%- model_class = Route -%>
<div class="span9">
  <div id="map-container">
    <div id="map-canvas">
    </div>
   </div>
    Sign up here<br><a href="http://www.dublinbikes.ie/Subscription" target="_blank"><img src='http://bicyclebuddy-193553.euw1-2.nitrousbox.com/assets/dublinbike.jpg' width="80" height="40"></a>
</div>
<div class="span3 routes-sidebar">

<table class="table table-striped">
  <thead>
    <tr>
    <th>Route</th>
      <th></th>
    </tr>
  </thead>
  <tbody>
    <% @routes.each do |route| %>
      <tr>
<!--         <td><%= link_to route.id, route_path(route) %></td> -->
        <td><%= link_to route.name, route_path(route) %></td>

        <td class="category-name"><%= route.category.name %></td>
<!--         <td><%=l route.created_at %></td>   -->
<!--         <td>
          <%= link_to t('.edit', :default => t("helpers.links.edit")),
                      edit_route_path(route), :class => 'btn btn-default btn-xs' %>
          <%= link_to t('.destroy', :default => t("helpers.links.destroy")),
                      route_path(route),
                      :method => :delete,
                      :data => { :confirm => t('.confirm', :default => t("helpers.links.confirm", :default => 'Are you sure?')) },
                      :class => 'btn btn-xs btn-danger' %>
        </td>  -->
      </tr>
    <% end %>
  </tbody>
</table>


</div>
